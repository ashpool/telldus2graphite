/*jshint undef:false */
var chai = require('chai'),
    chaiAsPromised = require('chai-as-promised');

chai.use(chaiAsPromised);

describe('graphite', function () {
    describe('#logMetric', function () {
/*        it('rejects with an Error', function (done) {
            var config = {},
                graphite = require('./../lib/graphite.js')(config),
                timestamp = 1427437171460,
                metric = {};

            graphite.logMetric(metric, timestamp).should.eventually.be.rejectedWith(Error).and.notify(done);
        });*/
    });
});
